# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Admin Panel Configuration (Opcional mas recomendado)
# A service role key bypassa o RLS e permite que o painel admin acesse todos os dados
# IMPORTANTE: Nunca exponha esta chave publicamente em produção!
# Use apenas para desenvolvimento local ou em um backend seguro
VITE_SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Deployment Notes:
# 1. Copie este arquivo para .env.local
# 2. Substitua os valores pelos dados reais do Supabase
# 3. Para o painel admin funcionar, adicione a VITE_SUPABASE_SERVICE_ROLE_KEY
#    Você pode encontrar esta chave em: Supabase Dashboard > Settings > API > service_role key
